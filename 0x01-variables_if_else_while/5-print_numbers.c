#include <stdio.h>
/**
 * main - Entry point
 * Description: Prints all single digits of base 10
 * Return: 0
 */
main int(void)
{
char i;
for (i = '0'; i <= '9'; ++i)
putchar(i);
putchar('\n');
return (0);
}
